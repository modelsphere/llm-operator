{{- if .Values.rbac.helpers.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "llmscaleoperator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "llmscaleoperator.resourceName" (dict "suffix" "llmscaler-viewer-role" "context" $) }}
rules:
- apiGroups:
  - autoscaling.modelsphere.dev
  resources:
  - llmscalers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - autoscaling.modelsphere.dev
  resources:
  - llmscalers/status
  verbs:
  - get
{{- end }}
