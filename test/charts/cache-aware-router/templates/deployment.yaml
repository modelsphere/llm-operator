apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-router
  labels:
    app: {{ .Release.Name }}-router
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-router
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-router
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      hostNetwork: true
      containers:
      - name: router
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        # Kubernetes doesn't have a direct equivalent to Docker's --ulimit flag on the Pod level.
        # The standard workaround is to use a shell to raise the soft limit before executing the binary.
        command: ["/bin/sh", "-c"]
        args: ["ulimit -n 65535 && exec /workspace/cache_aware_router"]
        volumeMounts:
        - name: config-volume
          mountPath: /workspace/configs
        resources:
{{ toYaml .Values.resources | indent 10 }}
      volumes:
      - name: config-volume
        configMap:
          name: {{ .Release.Name }}-router-config
